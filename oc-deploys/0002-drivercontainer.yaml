apiVersion: v1
kind: ServiceAccount
metadata:
  name: ice-driver-container
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ice-driver-container
rules:
- apiGroups:
  - security.openshift.io
  resources:
  - securitycontextconstraints
  verbs:
  - use
  resourceNames:
  - privileged
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ice-driver-container
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: ice-driver-container
subjects:
- kind: ServiceAccount
  name: ice-driver-container
userNames:
- system:serviceaccount:ice:ice-driver-container
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: ice-driver-container
spec:
  selector:
    matchLabels:
      app: ice-driver-container
  template:
    metadata:
      labels:
        app: ice-driver-container
    spec:
      serviceAccount: ice-driver-container
      serviceAccountName: ice-driver-container
      containers:
      - image: image-registry.openshift-image-registry.svc:5000/ice/ice-driver-container:demo
        name: ice-driver-container
        imagePullPolicy: Always
        command: ["/sbin/init"]
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "modprobe -r ice"]

        securityContext:
          privileged: true
      nodeSelector:
        node-role.kubernetes.io/worker: ""
